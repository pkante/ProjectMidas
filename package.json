{
  "name": "projectmidas",
  "version": "0.0.1",
  "main": "dist-electron/electron/main/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:electron": "tsc --project tsconfig.json",
    "build-all": "npm run build && npm run build:electron",
    "start": "npm run build:electron && electron dist-electron/electron/main/index.js"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "typescript": "^5.0.0",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "dotenv": "^17.0.1",
    "node-fetch": "^2.7.0"
  }
}
